runtime: custom
env: flex

service: default

instance_class: F2

automatic_scaling:
  min_num_instances: 1
  max_num_instances: 5
  cpu_utilization:
    target_utilization: 0.75

liveness_check:
  path: "/live"
  initial_delay_sec: 15

readiness_check:
  path: "/ready"
  check_interval_sec: 5
